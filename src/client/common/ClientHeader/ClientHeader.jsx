import React from 'react';
import styles from './client-header.scss';

const ClientHeader = props => {
    return (
        <div className={styles['client-header']}>
            Client Header
        </div>
    )
}

export default ClientHeader;